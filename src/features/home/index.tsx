import React from "react";
import { Banner, PageLayout } from "../../components";

export const HomePage: React.FC = () => {
    return (
        <PageLayout>
            <Banner />
        </PageLayout>
    );
};
